node_modules
dist
.next
apps/web/.next-dev
.turbo
.env
.env.local
.env.*.local
*.log
*.tsbuildinfo
.DS_Store
.vercel
.netlify

# Local design/plan notes (keep out of git)
docs/plans/

# Runtime data (db, uploads, etc.)
data/
apps/server/data/

# Tauri sidecar binaries (compiled per-host by pnpm --filter sidecar
# compile:tauri:host; regenerated as part of tauri build)
apps/desktop/src-tauri/binaries/

# Local AI tools / assistants metadata (machine-specific)
.adal/
.agent/
.agents/
.augment/
.claude/
.codex/
.codebuddy/
.commandcode/
.continue/
.cortex/
.crush/
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mux/
.neovate/
.openhands/
.pi/
.playwright-cli/
.playwright-mcp/
.pochi/
.qoder/
.qwen/
.roo/
.trae/
.trellis/
.vibe/
.windsurf/
.zencoder/

# AI framework generated files
AGENTS.md
# skills/ 下混着两类东西：外部同步来的 skill（`skills-lock.json` 登记，实际是
# 指向已被忽略的 .agents/ 的符号链接）和手写的项目文档 skills/openhorn/。原来
# 一条 `skills/` 把两类一起排除，导致 CLAUDE.md 当正式文档引用的 openhorn/ 从
# 未入库——换台机器 clone 下来那些引用全是死链。
# 必须写 `skills/*` 而不是 `skills/`：git 不允许在父目录已被排除的情况下重新
# 包含子项，写成 `skills/` 会让下面这条 `!` 完全失效。
skills/*
!skills/openhorn/
skills-lock.json
